# Cite-Before-Act-MCP
An MCP that for any tool call that mutates state (send email, charge card, delete file), force a “citation-first” dry-run with an LLM-readable preview; only on explicit approval does it execute.
