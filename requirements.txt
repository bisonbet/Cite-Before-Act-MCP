# Core dependencies
fastmcp>=0.9.0
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0

# JWT token handling with cryptographic support
# Required by MCP and Bot Framework (Teams)
# Note: Must be installed AFTER webexteamssdk to upgrade from its outdated PyJWT 1.7.1
pyjwt[crypto]>=2.10.1

# Approval platform integrations (install as needed)
# For Slack: pip install slack-sdk
slack-sdk>=3.27.0

# For Webex Teams: pip install webexteamssdk
# Note: webexteamssdk depends on PyJWT 1.7.1, but works fine with 2.10.1+
webexteamssdk>=1.6.0

# For Microsoft Teams: pip install botbuilder-core botframework-connector aiohttp
botbuilder-core>=4.15.0
botframework-connector>=4.15.0
aiohttp>=3.8.0

# Webhook server (required if using any approval platform)
flask>=3.0.0

